server:
  port: 9001

spring:
  application:
    name: USER-SERVICE
  datasource:
    url: jdbc:postgresql://userservicedb/users
    username: group18
    password: group18
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://serviceregistry:8761/eureka


keycloak:
  auth-server-url: http://keycloak:9004/auth
  realm: group18
  resource: user-service
  public-client: true
